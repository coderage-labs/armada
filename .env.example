# Node agent install token — set this after registering a node in the dashboard
# (Nodes → Add Node → copy the install token)
ARMADA_NODE_TOKEN=

# Optional: GitHub token for issue sync
# GITHUB_TOKEN=ghp_...

# Optional: Telegram bot for notifications
# TELEGRAM_BOT_TOKEN=
# TELEGRAM_CHAT_ID=
